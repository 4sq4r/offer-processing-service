spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/test
    username: test
    password: test
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5
      validation-timeout: 3000
  jpa:
    hibernate:
      ddl-auto: update