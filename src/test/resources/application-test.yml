spring:
  datasource:
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 5
      validation-timeout: 3000
  jpa:
    hibernate:
      ddl-auto: update